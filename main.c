/*
 * ============================================================================
 * main.c — Mini-Linux 程序入口
 * ============================================================================
 *
 * 整个项目是一个普通的 C 用户态程序，main 只做一件事：
 * 调用 kernel_boot() 进入模拟操作系统，直到用户 shutdown。
 *
 * 编译产物：mini_linux / mini_linux.exe
 * ============================================================================
 */

#include "kernel.h"

int main(void)
{
    kernel_boot();   /* 不会返回，直到 Shell 退出 */
    return 0;
}
